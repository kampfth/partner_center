# Partner Center v2 Backend - Environment Variables
# Copy to .env and fill in values

# Application
APP_ENV=production

# Supabase (use service_role key, NOT anon)
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Auth (optional - if empty, setup mode will generate and persist secret)
AUTH_TOTP_SECRET=
