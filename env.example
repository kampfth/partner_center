# Copy to `.env` on the server (Hostinger/FTP).
# IMPORTANT: `.env` must never be committed to Git and must be blocked from HTTP access via `.htaccess`.

APP_ENV=production

# Supabase Project
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co

# Server-side only (service_role). Never expose to frontend.
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY


